{% extends 'base-admin.html.twig' %}

{% block title %}Ajout Evenement{% endblock %}

{% block body %}
    <h1>Nouvel événement</h1>
    {#<form method="post" action="">
        <div class="col-md-5">
            <div class="form-group">
                <label for="title">Titre</label>
                <input type="text" name="title" id="title" class="form-control" placeholder="Entrez le titre">
            </div>
            <div class="form-group">
                <label for="dateOfClosure">Date de fin</label>
                <input type="date" name="dateOfClosure" id="dateOfClosure" class="form-control">
            </div>
            <div class="form-group">
                <label for="description">Description</label>
                <input type="text" name="description" id="description" class="form-control">
            </div>
            <button class="btn btn-success" type="submit"><img src="{{ asset('img/icon/valid.png') }}" class="icon"/>Valider</button>
        </div>
    </form>#}
    {{ form_start(form) }}
    <div class="col-md-6">
        <div class="form-group">
            {{ form_row(form.title) }}
        </div>
        <div class="form-group">
            {{ form_row(form.dateOfClosure) }}
        </div>
        <div class="form-group">
            {{ form_row(form.description) }}
        </div>
        <button class="btn btn-success" type="submit"><img src="{{ asset('img/icon/valid.png') }}" class="icon"/>Valider</button>
    </div>
    {{ form_end(form) }}
{% endblock %}