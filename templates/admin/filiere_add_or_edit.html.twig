{% extends 'admin/base-admin.html.twig' %}

{% block title %}AdminController{% endblock %}

{% block body %}

    {% if type == 'add' %}
        <h1 class="display-4 text-center">{{ "Ajouter une nouvelle filière"|upper }}</h1>
    {% else %}
        <h1 class="display-4 text-center">{{ "Editer cette filière"|upper }}</h1>
    {% endif %}

    <div class="row justify-content-center">
        <div class="col-12">

            {% for message in app.flashes('success') %}
                <div class="flash-notice">
                    <div class="alert alert-success" role="alert">
                        {{ message }}
                    </div>
                </div>
            {% endfor %}

            {{ form_start(pageForm) }}

            <div class="form-row mb-5">

                <div class="form-group col-6">
                    {{ form_label(pageForm.category) }}
                    {{ form_widget(pageForm.category, {'attr': {'class': 'form-control'}}) }}
                </div>

                <div class="form-group col-6">
                    {{ form_label(pageForm.colorPicker) }}
                    {{ form_widget(pageForm.colorPicker, {'attr': {'class': 'form-control'}}) }}
                </div>
                
            </div>

                <div class="form-row mb-5">

                <div class="form-group col-12">
                    {# {{ form_label(pageForm.title) }}#}
                    {{ form_widget(pageForm.title, {'attr': {'class': 'form-control'}}) }}
                </div>

                <div class="form-group col-12">
{#                    {{ form_label(pageForm.candidate) }}#}
                    {{ form_widget(pageForm.candidate, {'attr': {'class': 'form-control'}}) }}
                </div>
                <div class="form-group col-12">
{#                    {{ form_label(pageForm.description) }}#}
                    {{ form_widget(pageForm.description, {'attr': {'class': 'form-control'}}) }}
                </div>
            </div>

            <div class="form-row mb-5">
                <div class="form-group col-4">
{#                    {{ form_label(pageForm.titleBlock1) }}#}
                    {{ form_widget(pageForm.titleBlock1, {'attr': {'class': 'form-control'}}) }}
                    <br />
{#                    {{ form_label(pageForm.textBlock1) }}#}
                    {{ form_widget(pageForm.textBlock1, {'attr': {'class': 'form-control'}}) }}
                </div>

                <div class="form-group col-4">
{#                    {{ form_label(pageForm.titleBlock2) }}#}
                    {{ form_widget(pageForm.titleBlock2, {'attr': {'class': 'form-control'}}) }}
                    <br />
{#                    {{ form_label(pageForm.textBlock2) }}#}
                    {{ form_widget(pageForm.textBlock2, {'attr': {'class': 'form-control'}}) }}
                </div>

                <div class="form-group col-4">
{#                    {{ form_label(pageForm.titleBlock3) }}#}
                    {{ form_widget(pageForm.titleBlock3, {'attr': {'class': 'form-control'}}) }}
                    <br />
{#                    {{ form_label(pageForm.textBlock3) }}#}
                    {{ form_widget(pageForm.textBlock3, {'attr': {'class': 'form-control'}}) }}
                </div>
            </div>

            <div class="form-row mb-5">
                <div class="form-group col-6">
{#                    {{ form_label(pageForm.titleBlock4) }}#}
                    {{ form_widget(pageForm.titleBlock4, {'attr': {'class': 'form-control'}}) }}
                    <br />
{#                    {{ form_label(pageForm.textBlock4) }}#}
                    {{ form_widget(pageForm.textBlock4, {'attr': {'class': 'form-control'}}) }}
                </div>

                <div class="form-group col-6">
{#                    {{ form_label(pageForm.titleBlock5) }}#}
                    {{ form_widget(pageForm.titleBlock5, {'attr': {'class': 'form-control'}}) }}
                    <br />
{#                    {{ form_label(pageForm.textBlock5) }}#}
                    {{ form_widget(pageForm.textBlock5, {'attr': {'class': 'form-control'}}) }}
                </div>
            </div>

            <div class="form-row mb-5">
                <div class="form-group col-12">
                    {{ form_label(pageForm.video) }}
                    {{ form_widget(pageForm.video, {'attr': {'class': 'form-control'}}) }}
                    <p class="small"><em>Pour intégrer une vidéo Youtube, rendez-vous sur www.youtube.com, choisissez une vidéo, cliquer
                        sur le bouton <strong>partager</strong>, puis sur le bouton <strong>intégrer</strong>.
                        Un code de type "< iframe width="560" height="315" src="https://www.youtube.com/embed/nMblL-UsIDk" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></ iframe>"
                        va s'afficher. Copiez puis coller-le dans le champ ci-dessus.</em></p>
                </div>
            </div>

        </div>

        <div class="form-row mb-5">
            <div class="form-group col-12">
                <button type="submit" class="btn-lg btn-primary">
                    {% if type == 'add' %} Ajouter la filière {% else %} Editer la filière {% endif %}
                </button>
            </div>
        </div>

        {{ form_end(pageForm) }}
    </div>

{% endblock %}
